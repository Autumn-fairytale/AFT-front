import { styled } from '@mui/material';

export const CreateOrderFormStyled = styled('form')({
  display: 'flex',
  gap: '20px',

  height: '100%',

  paddingTop: '20px',
  paddingBottom: '20px',
});
