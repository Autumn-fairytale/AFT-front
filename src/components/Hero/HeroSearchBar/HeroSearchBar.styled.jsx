import { styled } from '@mui/material';

export const HeroSearchBarFormStyled = styled('form')(() => ({
  display: 'flex',
  gap: '10px',

  width: 'fit-content',
  padding: '10px',

  background: 'white',
  borderRadius: '10px',
}));
